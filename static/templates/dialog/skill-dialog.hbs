<div class="form-group custom-select">
  <label>{{localize "Characteristic"}}</label>
  <select name="characteristic">
    {{#select fields.characteristic}}
      {{#each (config "characteristics") as |label char|}}
        <option value="{{char}}">{{label}}</option>
      {{/each}}
    {{/select}}
  </select>
</div>