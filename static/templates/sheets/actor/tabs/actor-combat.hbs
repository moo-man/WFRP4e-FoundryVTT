<section class="tab {{tab.cssClass}}" data-group="primary" data-tab="{{tab.id}}">
    <div class="form-group">
        {{#if (settings "useGroupAdvantage")}}
            <label style="flex: 1"><a class="group-actions">{{localize "SHEET.GroupAdvantage"}} <i
                        class="fa-solid fa-chevron-down"></i></a></label>
        {{else}}
            <label style="flex: 1">{{localize "Advantage"}}</label>
        {{/if}}
        <div class="form-fields">
            <input type="number" name="system.status.advantage.value" value="{{system.status.advantage.value}}" />
        </div>
    </div>
    
    {{#if attacker}} 
    <div class="attacker">
        <div class="header">Attacker</div>
        <div class="details">
            <img src="{{tokenImg attacker.test.actor}}">
            <div class="test">
                <div>{{tokenName attacker.test.actor}}</div>
                <div>{{attacker.test.preData.title}}</div>
            </div>
            <a data-tooltip="{{localize 'SHEET.RemoveAttacker'}}"><i class="fas fa-times"></i></a>
        </div>
    </div>
    {{/if}}

    <div class="sheet-list">
        <div class="list-header row-content">
            <div class="list-name">{{localize "SHEET.MeleeWeaponHeader"}}
                <a class="unarmed list-button" data-tooltip="{{localize 'SHEET.Unarmed'}}">
                    <i class="fa fa-fist-raised"></i>
                </a>
                <a class="dodge list-button" data-tooltip="{{localize 'SHEET.Dodge'}}">
                    <i class="fas fa-reply"></i>
                </a>
                <a class="improvised list-button" data-tooltip="{{localize 'SHEET.Improvised'}}">
                    <i class="fas fa-question"></i>
                </a>
            </div>
            <div>{{localize "Group"}}</div>
            <div>{{localize "Damage"}}</div>
            <div>{{localize "Reach"}}</div>

        </div>
        <div class="list-content weapon-list">
            {{#each items.weapon}}
                {{#if this.system.isMelee}}
                    <div class="list-row" data-uuid="{{this.uuid}}">
                        <div class="row-content">
                            <div class="list-name">
                                <img src="{{this.img}}">
                                <a class="label">{{this.name}}</a>
                            </div>
                            <a>{{this.WeaponGroup}}</a>
                            <a class="weapon-damage {{#if this.damageToItem.value}}damaged{{/if}}" data-tooltip="{{localize 'SHEET.WeaponDamage'}}: {{this.damageToItem.value}}">+{{this.DamageString}}</a>
                            <a>{{this.Reach}}</a>
                        </div>
                        <div class="row-content">
                            {{#if this.twohanded.value}}
                                <div>{{localize "ITEM.TwoHanded"}}</div>
                            {{else if this.offhand.value}}
                                <a><i class="far fa-check-circle"></i> {{localize "SHEET.Offhand"}}</a>
                            {{else if ../actor.noOffhand}}
                                <a><i class="far fa-circle"></i> {{localize "SHEET.Offhand"}}</a>
                            {{/if}}
                            {{#if this.system.properties.special}}
                                <a>{{localize "Special"}}</a>
                            {{/if}}

                            {{#if this.system.Qualities.length}}
                                <div class="item-properties">
                                    <div class="property-icon">{{localize "SHEET.Qualities"}}</div>
                                    {{{array this.system.Qualities}}}
                                </div>
                            {{/if}}

                            {{#if this.system.UnusedQualities.length}}
                                <div class="item-properties" style="opacity : 0.5;">
                                    <div class="property-icon">{{localize "SHEET.Qualities"}}</div>
                                    {{{array this.system.UnusedQualities}}}
                                </div>
                            {{/if}}

                            {{#if this.system.InactiveQualities.length}}
                                <div class="item-properties" style="opacity : 0.5;">
                                    <div class="property-icon">{{localize "SHEET.Or"}}</div>
                                    {{{array this.system.InactiveQualities "inactive"}}}
                                </div>
                            {{/if}}

                            {{#if this.system.Flaws.length}}
                                <div class="item-properties">
                                    <div class="property-icon">{{localize "SHEET.Flaws"}}</div>
                                    {{{array this.system.Flaws}}}
                                </div>
                            {{/if}}
                        </div>
                    </div>
                {{/if}}
            {{/each}}
        </div>
    </div>

    <div class="sheet-list">
        <div class="list-header row-content">
            <div class="list-name">{{localize "SHEET.RangedWeaponHeader"}}</div>
            <div>{{localize "Group"}}</div>
            <div>{{localize "Ammunition"}}</div>
            <div>{{localize "Damage"}}</div>
            <div>{{localize "Range"}}</div>
        </div>
        <div class="list-content weapon-list">
            {{#each items.weapon}}
                {{#if this.system.isRanged}}
                    <div class="list-row" data-uuid="{{this.uuid}}">
                        <div class="row-content">
                            <div class="list-name">
                                <img src="{{this.img}}">
                                <a class="label">{{this.name}}</a>  
                            </div>
                            <a>{{this.WeaponGroup}}</a>
                            <select>
                            {{#select this.currentAmmo.value}}
                                <option value="">{{localize "NoneAmmo"}}</option>
                                {{#each this.ammoList as |ammo|}}
                                <option value="{{ammo.id}}"> ({{ammo.quantity.value}}) {{ammo.name}} {{#if ammo.location.name}}– {{ammo.location.name}}{{/if}}</option>
                                {{/each}}
                            {{/select}}
                            </select>
                            <a class="weapon-damage {{#if this.damageToItem.value}}damaged{{/if}}" data-tooltip="{{localize 'SHEET.WeaponDamage'}}: {{this.damageToItem.value}}">+{{this.DamageString}}</a>
                            <a>{{this.Reach}}</a>
                        </div>
                            <div class="row-content">
                            {{#if this.twohanded.value}}
                                <div>{{localize "ITEM.TwoHanded"}}</div>
                            {{else if this.offhand.value}}
                                <a><i class="far fa-check-circle"></i> {{localize "SHEET.Offhand"}}</a>
                            {{else if ../actor.noOffhand}}
                                <a><i class="far fa-circle"></i> {{localize "SHEET.Offhand"}}</a>
                            {{/if}}

                            {{#if this.system.loading}}
                                {{#if this.system.loaded.value}}
                                    {{#if this.system.repeater}}
                                        <a data-target="system.loaded.value">{{this.system.loaded.amt}}/{{this.system.loaded.max}}
                                            {{localize "Loaded"}}</a>
                                    {{else}}
                                        <a data-target="system.loaded.value"><i class="far fa-check-circle"></i> {{localize "Loaded"}}</a>
                                    {{/if}}
                                {{else}}
                                    {{#if this.system.repeater}}
                                        <a data-target="system.loaded.value">{{this.system.loaded.amt}}/{{this.system.loaded.max}}
                                            {{localize "Loaded"}}</a>
                                    {{else}}
                                        <a data-target="system.loaded.value"><i class="far fa-circle"></i> {{localize "Loaded"}}</a>
                                    {{/if}}
                                {{/if}}
                            {{/if}}


                            {{#if this.system.properties.special}}
                                <a>{{localize "Special"}}</a>
                            {{/if}}

                            {{#if this.system.properties.specialAmmo}}
                                <a>{{localize "Special Ammo"}}</a>
                            {{/if}}
                            {{#if this.system.Qualities.length}}
                                <div class="item-properties">
                                    <div class="property-icon">{{localize "SHEET.Qualities"}}</div>
                                    {{{array this.system.Qualities}}}
                                </div>
                            {{/if}}

                            {{#if this.system.UnusedQualities.length}}
                                <div class="item-properties" style="opacity : 0.5;">
                                    <div class="property-icon">{{localize "SHEET.Qualities"}}</div>
                                    {{{array this.system.UnusedQualities}}}
                                </div>
                            {{/if}}

                            {{#if this.system.InactiveQualities.length}}
                                <div class="item-properties" style="opacity : 0.5;">
                                    <div class="property-icon">{{localize "SHEET.Or"}}</div>
                                    {{{array this.system.InactiveQualities "inactive"}}}
                                </div>
                            {{/if}}

                            {{#if this.system.Flaws.length}}
                                <div class="item-properties">
                                    <div class="property-icon">{{localize "SHEET.Flaws"}}</div>
                                    {{{array this.system.Flaws}}}
                                </div>
                            {{/if}}
                        </div>
                    </div>
                {{/if}}
            {{/each}}
        </div>
    </div>
        
        <div class="armour">
            <div class="flexrow">
                {{> systems/wfrp4e/templates/partials/armour-location.hbs loc="head" locData=(lookup system.status.armour "head")}}
                {{> systems/wfrp4e/templates/partials/armour-location.hbs loc="body" locData=(lookup system.status.armour "body")}}
            </div>
            <div class="flexrow">
                {{> systems/wfrp4e/templates/partials/armour-location.hbs loc="lArm" locData=(lookup system.status.armour "lArm")}}
                {{> systems/wfrp4e/templates/partials/armour-location.hbs loc="rArm" locData=(lookup system.status.armour "rArm")}}
            </div>
            <div class="flexrow">
                {{> systems/wfrp4e/templates/partials/armour-location.hbs loc="lLeg" locData=(lookup system.status.armour "lLeg")}}
                {{> systems/wfrp4e/templates/partials/armour-location.hbs loc="rLeg" locData=(lookup system.status.armour "rLeg")}}
            </div>
        </div>


<div class="mount-section">
    {{#if actor.status.mount.mounted}}
        <div class="mounted">
            <div class="mount-header">
                {{localize "Mount"}} {{#if actor.status.mount.isToken}} ({{localize "Token"}}){{/if}}
            </div>
            <div class="mount-details">
                <div class="mount-img">
                    <img src="{{actor.mount.img}}">
                </div>
                <div class="mount-stats">
                    <div>
                        <span>{{actor.mount.name}}</span>
                        <label>{{localize "Name"}}</label>
                    </div>

                    <div>
                        <span> {{actor.mount.status.wounds.value}} / {{actor.mount.status.wounds.max}}</span>
                        <label>{{localize "Wounds"}}</label>
                    </div>
                </div>
                <div class="mount-controls">
                    <a data-tooltip="{{localize 'SHEET.RemoveMount'}}"><i class="fas fa-times"></i></a>
                    <a data-tooltip="{{localize 'SHEET.Dismount'}}"><i class="fas fa-arrow-down"></i></a>
                </div>
            </div>
        </div>
    {{else}}
        <div class="mount-drop" data-tooltip="Drag and drop a world Actor to act as a Mount.">
            {{localize 'Mount'}}
        </div>
    {{/if}}
</div>
</section>